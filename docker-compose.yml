services:
  shopnest:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
    environment:
      NODE_ENV: development
      PORT: ${PORT:-3000}
    restart: unless-stopped
